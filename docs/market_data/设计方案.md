# 股票数据源模块设计方案

**版本**：v3.0
**创建日期**：2025-01-01
**最后更新**：2025-01-02
**状态**：待审核
**作者**：Claude AI

---

## 文档修订记录

| 版本 | 日期 | 修订内容 | 修订人 |
|------|------|---------|--------|
| v1.0 | 2025-01-01 | 初始版本（多租户方案） | Claude AI |
| v2.0 | 2025-01-01 | 简化为管理员统一配置方案 | Claude AI |
| v3.0 | 2025-01-02 | 补充统一数据模型、ETL流程、数据同步策略、Tushare/AkShare接口说明、技术指标计算 | Claude AI |
| v4.0 | 2026-01-05 | 增加用户级数据源、双通道设计、限流策略、数据库设计独立成文档 | Claude AI |

---

## 目录

- [一、模块定位与目标](#一模块定位与目标)
- [二、核心概念界定](#二核心概念界定)
- [三、双通道数据流设计](#三双通道数据流设计)
- [四、用户级数据源配置](#四用户级数据源配置)
- [五、模块架构设计](#五模块架构设计)
- [六、统一数据模型与标准化](#六统一数据模型与标准化)
- [七、ETL 数据处理流程](#七etl-数据处理流程)
- [八、数据同步策略](#八数据同步策略)
- [九、Tushare 与 AkShare 接口说明](#九tushare-与-akshare-接口说明)
- [十、核心业务逻辑设计](#十核心业务逻辑设计)
- [十一、数据库设计](#十一数据库设计)
- [十二、技术指标计算](#十二技术指标计算)
- [十三、API 端点设计](#十三api-端点设计)
- [十四、与 TradingAgents 集成方案](#十四与-tradingagents-集成方案)
- [十五、并发与限流策略](#十五并发与限流策略)
- [十六、错误处理与降级](#十六错误处理与降级)
- [十七、定时任务设计](#十七定时任务设计)
- [十八、总结](#十八总结)

---

## 一、模块定位与目标

### 1.1 模块定位

股票数据源模块（`market_data`）是一个**全局数据服务模块**，负责：

- 管理员统一配置 A股/美股/港股 数据源
- 系统定时拉取数据并存储到数据库
- 为所有业务模块提供本地数据查询服务
- 数据过期时自动触发接口更新
- 支持多数据源降级（主数据源失败自动切换）

### 1.2 核心目标

| 目标 | 描述 |
|------|------|
| **统一管理** | 管理员配置数据源，所有用户共享使用 |
| **本地化存储** | 数据存储在本地数据库，用户请求不从外部接口获取 |
| **自动更新** | 定时任务自动拉取最新数据，过期数据自动触发更新 |
| **多数据源降级** | 主数据源失败时自动切换备用数据源 |
| **高可用性** | 分布式锁、限流、缓存等多重保障机制 |

### 1.3 数据流程

```
┌─────────────────────────────────────────────────────────┐
│ 数据流程图                                               │
└─────────────────────────────────────────────────────────┘

管理员配置数据源（一次性）
    ↓
系统定时拉取数据（定时任务）
    ├─ A股：每天收盘后拉取日K线
    ├─ 美股：每天收盘后拉取日K线
    └─ 港股：每天收盘后拉取日K线
    ↓
数据存入数据库（全局共享）
    ├─ stock_quotes（行情数据）
    ├─ stock_financials（财务数据）
    └─ stock_companies（公司信息）
    ↓
用户请求数据（从本地数据库读取）
    ├─ 数据存在且未过期 → 直接返回
    └─ 数据不存在或已过期 → 触发接口更新后返回
```

### 1.4 适用场景

- **TradingAgents 智能体分析**：提供股票行情、财报、公司信息等数据
- **智能选股模块**：批量获取多只股票数据
- **AI 问股模块**：查询股票信息
- **仪表板模块**：展示市场概况和个股数据

---

## 二、核心概念界定

### 2.1 数据源类型

本系统**只支持公共数据源**，由管理员统一配置和管理。

#### 2.1.1 A股数据源

**可用数据源**：
- **Tushare Pro**：需要管理员申请Token，积分制，数据最全面
- **AkShare**：完全免费开源，无需Token，数据覆盖全面
- **东方财富**：公开接口，免费
- **新浪财经**：公开接口，免费

**Tushare 特点**：
- 优势：数据最全（分钟线、Tick、百项财务指标）、接口最稳定、官方维护
- 劣势：需要积分，免费用户每日调用有限制（约200次/分钟）
- 推荐：作为主数据源（优先级1）

**AkShare 特点**：
- 优势：完全免费、开源、数据覆盖广、无需Token
- 劣势：基于爬虫，接口可能变动、稳定性中等
- 推荐：作为备用数据源（优先级2）

**数据类型**：
- 当日行情：延迟5-15分钟
- 历史日K线：可追溯到上市日
- 财务报表：季报、年报（三表完整）
- 公司信息：基本信息、行业分类、股本结构

#### 2.1.2 美股数据源

**可用数据源**：
- **Yahoo Finance**：免费接口，最常用
- **AkShare**：内部封装 Yahoo Finance，完全免费
- **Alpha Vantage（免费版）**：需要管理员申请API Key，限制较多

**Yahoo Finance 特点**：
- 优势：数据权威、覆盖全、稳定性高、完全免费
- 劣势：无官方Python SDK，需要使用第三方库（如 yfinance）
- 推荐：作为主数据源（优先级1）

**AkShare 特点**：
- 优势：封装了 Yahoo Finance，使用更简单
- 劣势：多一层封装，可能有延迟
- 推荐：作为备用数据源（优先级2）

**数据类型**：
- 当日行情：延迟15分钟（免费版）
- 历史日K线：可追溯数十年
- 财务报表：季报、年报
- 公司信息：基本信息、行业分类

#### 2.1.3 港股数据源

**可用数据源**：
- **AkShare**：完全免费，数据覆盖好
- **Yahoo Finance**：免费接口
- **东方财富（港股）**：公开接口

**AkShare 特点**：
- 优势：港股数据覆盖好、完全免费、数据实时性强
- 劣势：基于爬虫，可能有变动
- 推荐：作为主数据源（优先级1）

**Yahoo Finance 特点**：
- 优势：稳定性高、数据权威
- 劣势：港股数据不如 AkShare 全面
- 推荐：作为备用数据源（优先级2）

**数据类型**：
- 当日行情：延迟5-15分钟
- 历史日K线：可追溯到上市日
- 财务报表：年报、半年报
- 公司信息：基本信息

---

### 2.2 数据获取策略

#### 2.2.1 定时拉取（主动）

**策略**：系统定时任务自动拉取数据

```
A股数据更新时机：
├─ 日K线：每天 15:30（收盘后）
├─ 实时行情：交易时间内每5分钟
└─ 财务报表：每天 16:00 检查更新

美股数据更新时机：
├─ 日K线：每天 05:00（收盘后，北京时间）
├─ 实时行情：交易时间内每1分钟
└─ 财务报表：每天 06:00 检查更新

港股数据更新时机：
├─ 日K线：每天 16:30（收盘后）
├─ 实时行情：交易时间内每5分钟
└─ 财务报表：每天 17:00 检查更新
```

#### 2.2.2 按需更新（被动）

**触发条件**：
- 用户请求的数据不存在
- 数据已过期（超过TTL时间）

**流程**：
```
用户请求数据
    ↓
检查数据库
    ├─ 数据存在且未过期 → 直接返回
    └─ 数据不存在或已过期 → 触发接口更新
        ├─ 调用数据源接口
        ├─ 数据验证
        ├─ 存入数据库
        └─ 返回给用户
```

---

### 2.3 数据过期策略（TTL）

根据**天级预测**场景设计，不需要秒级/分钟级实时性。

| 数据类型 | TTL | 更新策略 | 存储表 |
|---------|-----|---------|--------|
| 当日行情 | 1天 | 每天收盘后更新 | stock_quotes |
| 历史日K线 | 永不过期 | 历史数据不变 | stock_klines |
| 财务报表 | 90天 | 季度检查更新 | stock_financials |
| 公司信息 | 30天 | 每月检查更新 | stock_companies |
| 历史新闻 | 永不过期 | 历史数据不变 | stock_news |
| 技术指标 | 跟随K线 | K线更新时重算 | stock_klines.indicators |

**TTL判断逻辑**：
```
数据是否过期：

场景1：历史K线、历史新闻
└─ 永不过期，一旦入库就不再更新

场景2：当日行情
└─ 如果（当前日期 != 数据日期）→ 过期

场景3：财务报表、公司信息
└─ 如果（当前时间 - fetched_at）> TTL → 过期

过期数据处理：
├─ 过期但不删除，保留历史记录
└─ 触发接口更新，获取最新数据
```

---

### 2.4 数据源功能对比

| 功能模块 | Tushare Pro | AkShare | 优先级建议 |
|---------|-------------|---------|-----------|
| **A股日K线** | ✅ 最全（支持复权） | ✅ 全面（支持复权） | Tushare > AkShare |
| **A股分钟线** | ✅ 需2000积分 | ✅ 免费 | AkShare > Tushare |
| **A股财务报表** | ✅ 最详细（百项指标） | ✅ 基本指标 | Tushare > AkShare |
| **港股日K线** | ✅ 需2000积分 | ✅ 免费 | AkShare > Tushare |
| **美股日K线** | ✅ 需2000积分 | ✅ 免费（Yahoo） | AkShare > Tushare |
| **新闻公告** | ✅ 官方公告 | ❌ 不支持 | Tushare 独占 |
| **龙虎榜** | ✅ 官方数据 | ✅ 实时数据 | Tushare > AkShare |
| **资金流向** | ✅ 官方数据 | ✅ 实时数据 | Tushare > AkShare |
| **板块数据** | ✅ 完整 | ✅ 完整 | AkShare（更新快） |
| **交易日历** | ✅ 官方 | ❌ 较弱 | Tushare 独占 |

---

## 三、双通道数据流设计

### 3.1 设计背景

为了满足不同用户对数据实时性和成本的需求，系统设计了**双通道数据流**：

- **通道1（直接返回）**：数据直接返回给业务模块，不存储，不限流
- **通道2（存入数据库）**：数据存入系统数据库，按股票+数据类型+1分钟限流

### 3.2 双通道工作原理

#### 3.2.1 场景1：用户使用免费数据源（项目信息源）

```
┌─────────────────────────────────────────────┐
│ 用户选择"项目信息源"                         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 从系统数据库读取数据                         │
│ - Tushare（免费额度）                        │
│ - AkShare（完全免费）                        │
│ - 数据可能略有延迟（通常<1天）               │
└─────────────────────────────────────────────┘
                    ↓
            返回给业务模块使用
```

#### 3.2.2 场景2：用户使用个人付费数据源（实时信息源）

```
┌─────────────────────────────────────────────┐
│ 用户选择"实时信息源"                         │
│ （用户自己配置了付费API Key）                │
└─────────────────────────────────────────────┘
                    ↓
        调用用户配置的付费接口
                    ↓
        ┌───────────┴───────────┐
        ↓                       ↓
┌───────────────┐       ┌───────────────┐
│  通道1        │       │  通道2        │
│  直接返回     │       │  存入数据库   │
│  不限流       │       │  限流控制     │
└───────────────┘       └───────────────┘
        ↓                       ↓
   直接返回给工具模块     写入系统数据库
   用于AI分析             供其他用户使用
```

### 3.3 通道2的限流策略

#### 3.3.1 限流粒度

按 **股票代码 + 数据类型** 进行限流：

| 限流键 | 说明 | 示例 |
|--------|------|------|
| `symbol + data_type` | 同一股票的同一数据类型 | `000001.SZ + quote`（行情数据）<br>`000001.SZ + financial`（财务数据）<br>`000001.SZ + company`（公司信息） |

#### 3.3.2 时间窗口

**1分钟时间窗口**：
- 如果某只股票的某种数据类型在1分钟内已经写过
- 后续的写入请求会被拒绝
- 1分钟后允许再次写入

#### 3.3.3 并发控制场景

**场景：A用户和B用户同时分析000001.SZ**

```
时间线：
├─ T0秒：A用户和B用户同时发起分析（都使用实时信息源）
├─ T0.1秒：两人都调用用户配置的API接口（通道1，不限流，都调用成功）
├─ T0.5秒：两人都获取到数据
│
├─ 通道1（直接返回，不限流）：
│   ├─ A用户的数据 → 工具模块 → AI分析
│   └─ B用户的数据 → 工具模块 → AI分析
│   （并行执行，互不影响）
│
└─ 通道2（存入数据库，限流）：
    ├─ A用户尝试写入000001.SZ的quote数据
    │   ├─ 检查限流记录：不存在
    │   ├─ 允许写入 → 写入成功
    │   └─ 记录限流：000001.SZ + quote（过期时间1分钟）
    │
    └─ B用户尝试写入000001.SZ的quote数据
        ├─ 检查限流记录：存在且未过期
        ├─ 拒绝写入 → 数据丢弃
        └─ B用户的通道2数据不写入数据库
```

**时间差场景**：
```
时间线：
├─ T0秒：A用户分析000001.SZ → 写入成功 → 记录限流（过期时间T0+60秒）
├─ T20秒：B用户分析000001.SZ → 检查限流 → 在1分钟内 → 拒绝写入
├─ T70秒：C用户分析000001.SZ → 检查限流 → 已超过1分钟 → 允许写入
```

### 3.4 数据一致性说明

#### 3.4.1 盘中数据 vs 收盘数据

**盘中数据**：
- 交易时间内获取的数据（如14:30）
- 数据字段 `is_complete: false`（未收盘）
- 可能是未完成的当日行情

**收盘数据**：
- 收盘后获取的完整数据（15:30后）
- 数据字段 `is_complete: true`（已收盘）
- 完整的日K线数据

**写入数据库时**：
- 用户在14:30调用接口获取盘中数据 → `is_complete: false`
- 定时任务在15:30获取收盘数据 → `is_complete: true`
- 用户使用"项目信息源"时优先返回 `is_complete: true` 的数据

#### 3.4.2 数据允许差异

**原则**：实时信息源和项目信息源的数据可能不同，这是允许的。

- 用户选择"实时信息源"：获取的是接口实时返回的数据（可能是盘中数据）
- 用户选择"项目信息源"：获取的是数据库中的数据（可能是昨天的收盘数据）
- 单次任务只能选择一个数据源

### 3.5 降级逻辑

**用户使用"实时信息源"时的降级流程**：

```
用户发起分析任务（选择实时信息源）
    ↓
检查用户是否配置了该市场的实时数据源
    ↓
├─ 未配置 → 使用"项目信息源"（从数据库获取）
│
└─ 已配置 → 调用用户配置的接口
    ├─ 调用成功
    │   ├─ 通道1：直接返回（不限流）
    │   └─ 通道2：尝试写入数据库（限流）
    │
    └─ 调用失败 → 降级到"项目信息源"（从数据库获取）
```

---

## 四、用户级数据源配置

### 4.1 用户级数据源定位

**配置者**：每个用户自己
**可见性**：仅用户自己可见
**使用场景**：TradingAgents选择"实时信息源"时
**数据存储**：直接返回，不存储（通道1）；存入数据库，限流（通道2）

### 4.2 配置规则

#### 4.2.1 允许配置的付费数据源

**原则**：只展示需要付费的接口让用户配置，免费数据源不允许用户添加。

| 市场 | 数据源 | 是否需要Key | 说明 |
|------|--------|------------|------|
| A股 | Tushare Pro | ✅ 需要积分 | 用户自己的Token |
| 美股 | Alpha Vantage | ✅ 需要Key | 免费版限制较多 |
| 港股 | iTick | ✅ 需要Key | 付费数据源 |

**不允许用户配置的免费数据源**：
- ❌ AkShare（完全免费）
- ❌ Yahoo Finance（免费API）
- ❌ 东方财富（公开接口）

**原因**：免费数据源使用系统公共配置即可，避免重复配置。

#### 4.2.2 配置粒度

按市场分别配置（A股、美股、港股）：

**配置结构**：
```yaml
user_data_sources:
  A_STOCK:
    - source_id: "tushare_pro"
      api_key: "用户自己的Tushare Token"
      enabled: true
      priority: 1

  US_STOCK:
    - source_id: "alpha_vantage"
      api_key: "用户自己的Alpha Vantage Key"
      enabled: true
      priority: 1

  HK_STOCK:
    - source_id: "itick"
      api_key: "用户自己的iTick Key"
      enabled: true
      priority: 1
```

### 4.3 与系统级数据源的区别

| 对比项 | 系统级数据源（公共） | 用户级数据源（个人） |
|--------|-------------------|-------------------|
| **配置者** | 超级管理员 | 每个用户自己 |
| **可见性** | 所有用户共享 | 仅用户自己可见 |
| **API Key** | 公共Key | 用户自己的Key |
| **数据源类型** | 免费为主（Tushare免费额度、AkShare） | 仅付费接口 |
| **数据存储** | 存入数据库，所有用户共享 | 通道1：直接返回<br>通道2：存入数据库（限流） |
| **使用场景** | 所有模块（AI问答、选股、TradingAgents项目信息源） | TradingAgents（实时信息源） |
| **成本** | 系统承担 | 用户自己承担 |

### 4.4 用户级数据源的价值

**为用户提供的价值**：
- 获取最新实时数据（可能是盘中数据）
- 不受系统公共数据源的限流限制
- 数据存入数据库后，其他使用"项目信息源"的用户也能获取到相对实时的数据

**为系统提供的价值**：
- 减轻系统公共数据源的API调用压力
- 用户付费数据源的数据进入数据库，丰富数据维度
- 用户配置了实时数据源后，分析任务不消耗系统配额

---

## 五、模块架构设计

### 5.1 目录结构

```
backend/modules/market_data/
├── __init__.py
├── admin_api.py                            # 管理员 API（配置管理）
│
├── config/
│   ├── __init__.py
│   ├── schemas.py                          # 配置数据模型
│   └── service.py                          # 配置管理服务
│
├── sources/
│   ├── __init__.py
│   ├── base.py                             # 数据源抽象基类
│   ├── registry.py                         # 数据源注册表
│   ├── metadata/
│   │   └── sources_manifest.yaml           # 数据源清单
│   ├── a_stock/
│   │   ├── __init__.py
│   │   ├── tushare_adapter.py              # Tushare
│   │   ├── eastmoney_adapter.py            # 东方财富
│   │   └── sina_adapter.py                 # 新浪财经
│   ├── us_stock/
│   │   ├── __init__.py
│   │   ├── yahoo_adapter.py                # Yahoo Finance
│   │   └── alphavantage_adapter.py         # Alpha Vantage
│   └── hk_stock/
│       ├── __init__.py
│       ├── yahoo_adapter.py                # Yahoo Finance
│       └── eastmoney_adapter.py            # 东方财富（港股）
│
├── models/
│   ├── __init__.py
│   ├── data_config.py                      # 数据源配置模型
│   ├── stock_quote.py                      # 行情数据模型
│   ├── stock_financials.py                 # 财务数据模型
│   └── stock_company.py                    # 公司信息模型
│
├── services/
│   ├── __init__.py
│   ├── data_fetcher.py                     # 数据获取服务（按需更新）
│   ├── data_scheduler.py                   # 定时任务调度器
│   ├── storage_service.py                  # 数据存储服务
│   ├── rate_limiter.py                     # 限流服务
│   ├── lock_service.py                     # 分布式锁服务
│   └── validator.py                        # 数据验证服务
│
├── tools/
│   ├── __init__.py
│   ├── base_tool.py                        # 工具基类
│   ├── a_stock_tools.py                    # A股工具
│   ├── us_stock_tools.py                   # 美股工具
│   └── hk_stock_tools.py                   # 港股工具
│
└── tests/
    ├── test_adapters.py
    └── test_data_fetcher.py
```

### 3.2 模块职责划分

| 子模块 | 职责 |
|--------|------|
| **config/** | 数据源配置的 CRUD（管理员专用） |
| **sources/** | 数据源适配器实现，统一接口抽象 |
| **models/** | 数据库模型定义 |
| **services/** | 核心业务逻辑（获取、调度、验证、限流） |
| **tools/** | TradingAgents 工具集成 |
| **admin_api.py** | 管理员配置 API（用户不可见） |

---

### 3.3 数据源适配器说明

#### 3.3.1 适配器设计原则

**为什么按市场分目录**：
- 不同市场的数据特点完全不同（A股/美股/港股）
- 每个市场可能有3-4个数据源，每个适配器文件负责一个数据源
- 便于管理和维护，新增数据源时只需添加对应市场的适配器文件

**目录组织逻辑**：
```
sources/
├─ a_stock/          # A股数据源目录
│  ├─ tushare_adapter.py    # Tushare 适配器（500+ 行）
│  ├─ akshare_adapter.py    # AkShare 适配器（400+ 行）
│  ├─ eastmoney_adapter.py  # 东方财富适配器（300+ 行）
│  └─ sina_adapter.py        # 新浪财经适配器（200+ 行）
├─ us_stock/         # 美股数据源目录
│  ├─ yahoo_adapter.py       # Yahoo Finance 适配器
│  └─ akshare_adapter.py     # AkShare（美股）适配器
└─ hk_stock/         # 港股数据源目录
   ├─ yahoo_adapter.py       # Yahoo Finance 适配器
   ├─ akshare_adapter.py     # AkShare（港股）适配器
   └─ eastmoney_adapter.py   # 东方财富（港股）适配器
```

**每个适配器的职责**：
1. 封装对应数据源的API调用
2. 实现字段映射（原始字段 → 标准字段）
3. 实现数据类型转换
4. 实现单位标准化（手→股，千元→元）
5. 实现股票代码标准化
6. 实现数据验证

#### 3.3.2 适配器接口规范

**所有适配器必须实现的方法**：

```
DataSourceAdapter（抽象基类）
├─ get_daily_quote(symbol, date)        # 获取单日行情
├─ get_klines(symbol, start, end)       # 获取历史K线
├─ get_financials(symbol, period)       # 获取财务报表
├─ get_company_info(symbol)             # 获取公司信息
├─ get_news(symbol, start, end)         # 获取新闻
└─ normalize_data(raw_data, data_type)  # 数据标准化
```

**标准化流程**：
```
原始数据 → 字段映射 → 类型转换 → 单位标准化 → 代码标准化 → 标准数据
```

---

## 四、统一数据模型与标准化

### 4.1 设计原则

**核心问题**：不同数据源返回的数据格式、字段名、数据类型各不相同，需要统一为标准格式后才能存入数据库。

**设计原则**：
1. **以 Tushare 为主要参考标准**：Tushare 数据最全面，字段定义最规范
2. **统一字段命名**：采用小写下划线命名法（snake_case）
3. **统一数据类型**：价格用 float，成交量用 int，日期用 YYYY-MM-DD 字符串
4. **统一股票代码格式**：`{代码}.{市场后缀}`，如 `600000.SH`、`AAPL.US`、`0700.HK`

---

### 4.2 股票代码标准化规范

#### 4.2.1 标准格式定义

```
标准格式：{股票代码}.{市场后缀}

市场后缀规范：
├─ A股（上海）：.SH
├─ A股（深圳）：.SZ
├─ A股（北交所）：.BJ
├─ 美股：.US
├─ 港股：.HK
```

#### 4.2.2 各数据源代码格式转换

| 数据源 | 原始格式 | 标准格式 | 转换规则 |
|--------|---------|---------|---------|
| **Tushare** | `600000.SH` | `600000.SH` | 无需转换（已是标准格式） |
| **AkShare** | `sh600000` | `600000.SH` | 去掉前缀 sh/sz，添加后缀 .SH/.SZ |
| **东方财富** | `1.600000` | `600000.SH` | 1=上海，0=深圳，解析后转换 |
| **新浪财经** | `sh600000` | `600000.SH` | 同 AkShare |
| **Yahoo Finance** | `600000.SS` | `600000.SH` | `.SS` → `.SH`，`.SZ` 保持 |
| **Yahoo Finance（美股）** | `AAPL` | `AAPL.US` | 添加 `.US` 后缀 |
| **Yahoo Finance（港股）** | `0700.HK` | `0700.HK` | 无需转换 |

---

### 4.3 统一数据字段定义

#### 4.3.1 日K线数据标准字段

| 标准字段名 | 类型 | 必填 | 说明 |
|-----------|------|------|------|
| `symbol` | string | ✅ | 标准化股票代码 |
| `market` | string | ✅ | 市场类型：A_STOCK / US_STOCK / HK_STOCK |
| `trade_date` | string | ✅ | 交易日期，格式：YYYY-MM-DD |
| `open` | float | ✅ | 开盘价 |
| `high` | float | ✅ | 最高价 |
| `low` | float | ✅ | 最低价 |
| `close` | float | ✅ | 收盘价 |
| `volume` | int | ✅ | 成交量（股） |
| `amount` | float | ❌ | 成交额（元） |
| `turnover_rate` | float | ❌ | 换手率 |
| `change_pct` | float | ❌ | 涨跌幅（%） |
| `pre_close` | float | ❌ | 昨收价 |

#### 4.3.2 Tushare 字段映射表

| Tushare 字段 | 标准字段 | 转换规则 |
|-------------|---------|---------|
| `ts_code` | `symbol` | 直接使用 |
| `trade_date` | `trade_date` | 格式化为 YYYY-MM-DD |
| `open` | `open` | 直接使用 |
| `high` | `high` | 直接使用 |
| `low` | `low` | 直接使用 |
| `close` | `close` | 直接使用 |
| `vol` | `volume` | 乘以 100（Tushare 单位是手，1手=100股） |
| `amount` | `amount` | 乘以 1000（Tushare 单位是千元） |
| `pct_chg` | `change_pct` | 直接使用 |
| `pre_close` | `pre_close` | 直接使用 |

#### 4.3.3 AkShare 字段映射表

| AkShare 字段 | 标准字段 | 转换规则 |
|-------------|---------|---------|
| `代码` / `股票代码` | `symbol` | 标准化转换（见 4.2.2） |
| `日期` | `trade_date` | 格式化为 YYYY-MM-DD |
| `开盘` | `open` | 直接使用 |
| `最高` | `high` | 直接使用 |
| `最低` | `low` | 直接使用 |
| `收盘` | `close` | 直接使用 |
| `成交量` | `volume` | 直接使用（单位已是股） |
| `成交额` | `amount` | 直接使用 |
| `涨跌幅` | `change_pct` | 直接使用 |
| `换手率` | `turnover_rate` | 直接使用 |

---

### 4.4 数据类型分类

根据数据量大小，将数据分为**轻量数据**和**重量数据**：

#### 4.4.1 轻量数据（定时同步）

| 数据类型 | 单条大小 | 总量估算 | 说明 |
|---------|---------|---------|------|
| 公司基本信息 | ~1KB | A股5000只 ≈ 5MB | 变化频率低 |
| 最新行情（当日） | ~0.5KB | 5000只 ≈ 2.5MB | 每日更新 |
| 最新财务指标 | ~2KB | 5000只 ≈ 10MB | 季度更新 |

#### 4.4.2 重量数据（按需同步）

| 数据类型 | 单条大小 | 总量估算（单只股票1年） | 说明 |
|---------|---------|----------------------|------|
| 历史日K线 | ~0.2KB/条 | 250条 ≈ 50KB | 每日一条 |
| 历史新闻 | ~5KB/条 | 500条 ≈ 2.5MB | 数量不定 |
| 历史财务报表 | ~10KB/条 | 4条 ≈ 40KB | 每季度一条 |

---

## 五、ETL 数据处理流程

### 5.1 ETL 概述

ETL（Extract-Transform-Load）是将多数据源数据标准化并存入数据库的核心流程。

```
ETL 流程概览：

┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Extract   │ ──→ │  Transform  │ ──→ │    Load     │
│   (提取)     │     │   (转换)    │     │   (加载)    │
└─────────────┘     └─────────────┘     └─────────────┘
      │                    │                    │
      ↓                    ↓                    ↓
从数据源获取         字段映射、清洗          存入数据库
   原始数据           格式标准化              更新索引
```

---

### 5.2 Extract（提取）阶段

```
提取流程：

1. 根据市场类型获取数据源优先级列表
   ├─ A股：[Tushare, AkShare, 东方财富]
   ├─ 美股：[Yahoo Finance, AkShare]
   └─ 港股：[Yahoo Finance, AkShare, 东方财富]

2. 按优先级依次尝试获取数据
   ├─ 优先级1成功 → 返回原始数据
   └─ 优先级1失败 → 尝试优先级2
       ├─ 优先级2成功 → 返回原始数据
       └─ 优先级2失败 → 尝试优先级3

3. 记录数据来源
   └─ 返回：{ raw_data, source_id, fetch_time }
```

---

### 5.3 Transform（转换）阶段

#### 5.3.1 转换流程

```
转换流程：

原始数据
    ↓
步骤1：字段映射
├─ 根据数据源选择对应的映射规则
├─ 将原始字段名转换为标准字段名
└─ 处理字段缺失（使用 null 或默认值）
    ↓
步骤2：数据类型转换
├─ 价格字段 → float（保留2位小数）
├─ 成交量 → int
├─ 日期 → YYYY-MM-DD 字符串
└─ 百分比 → float（如 5.23 表示 5.23%）
    ↓
步骤3：单位标准化
├─ Tushare 成交量（手）→ 股：× 100
├─ Tushare 成交额（千元）→ 元：× 1000
└─ 其他数据源检查并统一
    ↓
步骤4：股票代码标准化
├─ 识别原始代码格式
├─ 转换为标准格式：{代码}.{后缀}
└─ 验证代码有效性
    ↓
步骤5：数据清洗
├─ 去除空白字符
├─ 处理异常值（价格为0、负数等）
└─ 处理缺失值
    ↓
步骤6：数据验证
├─ 完整性检查（必填字段）
├─ 合理性检查（价格>0，成交量>=0）
└─ 一致性检查（high>=low）
    ↓
标准化数据
```

#### 5.3.2 异常数据处理规则

| 异常类型 | 处理方式 |
|---------|---------|
| 价格为 0 | 使用前一日收盘价填充，或标记为无效 |
| 价格为负数 | 拒绝该条数据 |
| 成交量为负数 | 拒绝该条数据 |
| high < low | 拒绝该条数据 |
| 日期格式错误 | 尝试解析，失败则拒绝 |
| 必填字段缺失 | 尝试从其他数据源补充，否则拒绝 |

---

### 5.4 Load（加载）阶段

```
加载流程：

标准化数据
    ↓
步骤1：去重检查
├─ 查询数据库是否已存在相同记录
├─ 唯一键：symbol + trade_date（行情数据）
└─ 存在 → 更新；不存在 → 插入
    ↓
步骤2：写入数据库
├─ 添加元数据字段
│   ├─ data_source: 数据来源
│   ├─ fetched_at: 获取时间
│   ├─ data_quality: 数据质量评分
│   └─ expires_at: 过期时间
└─ 执行插入/更新操作
    ↓
步骤3：更新索引（自动）
    ↓
步骤4：记录操作日志
├─ 成功数量
├─ 失败数量
└─ 失败原因
```

---

## 六、数据同步策略

### 6.1 同步类型概览

| 同步类型 | 触发方式 | 同步内容 | 数据范围 |
|---------|---------|---------|---------|
| **定时同步** | 定时任务 | 轻量数据 | 所有股票 |
| **自选股同步** | 用户添加自选 | 完整数据 | 指定股票 |
| **按需同步** | 数据缺失/过期 | 指定数据 | 指定股票 |

---

### 6.2 定时同步策略

**同步目标**：所有股票的轻量数据（基础数据）

**同步内容**：
- ✅ 当日行情数据（开高低收量）
- ✅ 公司基本信息（变化时更新）
- ✅ 最新财务指标
- ❌ 历史日K线（数据量大，不定时同步）
- ❌ 历史新闻（数据量大，不定时同步）

```
定时同步时间表：

A股市场：
├─ 15:30 - 同步当日行情
├─ 16:00 - 检查并更新公司信息（每周一次）
└─ 16:30 - 检查并更新财务指标（每日）

美股市场：
├─ 05:00 - 同步当日行情（北京时间）
├─ 05:30 - 检查并更新公司信息（每周一次）
└─ 06:00 - 检查并更新财务指标（每日）

港股市场：
├─ 16:30 - 同步当日行情
├─ 17:00 - 检查并更新公司信息（每周一次）
└─ 17:30 - 检查并更新财务指标（每日）
```

---

### 6.3 自选股同步策略

**触发时机**：用户添加股票到自选列表

**同步内容**：
- ✅ 公司基本信息
- ✅ 历史日K线（时间范围由用户配置，默认1年）
- ✅ 历史财务报表（近4期）
- ✅ 历史新闻（时间范围由用户配置，默认1年）
- ✅ 技术指标计算

**用户可配置项**：

| 配置项 | 默认值 | 可选范围 | 说明 |
|--------|-------|---------|------|
| 日K线历史范围 | 1年 | 3个月/6个月/1年/2年/3年 | 同步多长时间的历史K线 |
| 历史新闻范围 | 1年 | 3个月/6个月/1年/2年 | 同步多长时间的历史新闻 |
| 财务报表期数 | 4期 | 4期/8期/12期 | 同步多少期财务报表 |

---

### 6.4 按需同步策略

**触发时机**：
1. 用户请求的数据在数据库中不存在
2. 用户请求的数据已过期（超过 TTL）
3. AI 工具调用需要数据但数据缺失

**这就是「缓存命中」概念**：数据库作为数据中转站，首次请求触发同步，后续请求直接复用。

```
按需同步流程：

用户/工具请求数据
    ↓
检查数据库
├─ 数据存在且未过期 → 直接返回（缓存命中）
└─ 数据不存在或已过期 → 触发同步
    ↓
获取分布式锁
├─ 获取成功 → 执行同步
│   ├─ 调用数据源API
│   ├─ ETL处理
│   ├─ 存入数据库
│   └─ 释放锁
└─ 获取失败 → 等待2秒后重新检查数据库
    └─ 其他请求已完成同步，直接返回
    ↓
返回数据给用户/工具
```

---

## 七、Tushare 与 AkShare 接口说明

### 7.1 Tushare 接口

#### 7.1.1 基本信息

| 项目 | 说明 |
|------|------|
| **官网** | [https://tushare.pro](https://tushare.pro) |
| **类型** | 免费（需积分）+ 付费 |
| **积分获取** | 注册送120积分，邀请/捐赠可获更多 |
| **调用限制** | 免费用户200次/分钟，付费用户更高 |
| **数据覆盖** | A股、港股、美股、期货、基金、债券、指数 |
| **数据质量** | ⭐⭐⭐⭐⭐（最高） |

#### 7.1.2 核心功能

**支持的数据类型**：

1. **日K线数据** - 提供历史日线数据，支持复权，权限要求120积分（免费用户可用）
2. **实时行情** - 交易日15:30后可获取当日数据，T+0延迟
3. **财务报表** - 利润表、资产负债表、现金流量表，权限要求120积分以上
4. **公司信息** - 基本信息、行业分类、上市日期等
5. **交易日历** - 官方交易日历数据
6. **龙虎榜数据** - 官方龙虎榜数据
7. **资金流向** - 资金流向数据
8. **板块数据** - 行业板块、概念板块数据

> 详细接口说明请参考：[Tushare 接口文档](./tushare接口文档.md)

---

### 7.2 AkShare 接口

#### 7.2.1 基本信息

| 项目 | 说明 |
|------|------|
| **官网** | [https://akshare.akfamily.xyz](https://akshare.akfamily.xyz) |
| **类型** | 完全免费开源 |
| **调用限制** | 无硬性限制，建议控制频率避免被封IP |
| **数据覆盖** | A股、港股、美股、期货、基金、债券、指数、宏观经济 |
| **数据质量** | ⭐⭐⭐⭐（高） |
| **数据来源** | 新浪财经、东方财富、腾讯财经等公开接口 |

#### 7.2.2 核心功能

**支持的数据类型**：

1. **A股日K线** - 支持前复权、后复权，数据来源东方财富
2. **实时行情** - 返回所有A股实时行情，更新频率5分钟级别
3. **财务报表** - 利润表、资产负债表、现金流量表，数据来源新浪财经、东方财富
4. **公司信息** - 公司名称、上市日期、行业、地域等基本信息
5. **港股数据** - 日K线、实时行情、公司信息
6. **美股数据** - 通过 Yahoo Finance 获取美股数据
7. **基金数据** - 场内基金、ETF 数据
8. **期货数据** - 商品期货、股指期货数据

> 详细接口说明请参考：[AkShare 接口文档](./akshare接口文档.md)

---

### 7.3 Tushare vs AkShare 对比

| 对比项 | Tushare | AkShare |
|--------|---------|---------|
| **费用** | 积分制（免费+付费） | 完全免费 |
| **数据质量** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **稳定性** | 高（官方API） | 中（爬虫接口可能变） |
| **A股数据** | 最全（分钟线、Tick） | 全（日线、实时行情） |
| **财务数据** | 最全（三表+百项指标） | 全（三表+常用指标） |
| **美股数据** | 支持（需更高积分） | 通过 Yahoo Finance |
| **历史数据** | 可追溯到上市日 | 可追溯到上市日 |
| **推荐优先级** | A股首选 | A股备用，港股/美股补充 |

---

### 7.4 推荐使用策略

**A股数据**：
- 优先级1：Tushare（数据最全、最稳定）
- 优先级2：AkShare（免费备用）
- 优先级3：东方财富直接接口（降级）

**美股数据**：
- 优先级1：Yahoo Finance
- 优先级2：AkShare（内部调用 Yahoo Finance）

**港股数据**：
- 优先级1：AkShare
- 优先级2：Yahoo Finance

---

### 7.5 数据源选择策略

#### 7.5.1 日K线数据

**选择策略**：

| 数据源 | 优势 | 劣势 | 适用场景 |
|--------|------|------|---------|
| **Tushare** | 支持复权、数据稳定、权限要求低（0积分） | 需要积分 | A股历史K线首选 |
| **AkShare** | 完全免费、支持多市场 | 基于爬虫，稳定性中等 | A股备用、港股/美股 |

**字段映射**：Tushare 见 4.3.2节，AkShare 见 4.3.3节

---

#### 7.5.2 财务报表数据

**选择策略**：

| 数据源 | 字段数量 | 权限要求 | 推荐度 |
|--------|---------|---------|--------|
| **Tushare** | 100+字段/表 | 2000积分 | ⭐⭐⭐⭐⭐（数据最全） |
| **AkShare** | 30-40字段/表 | 完全免费 | ⭐⭐⭐⭐（备用） |

**降级策略**：
- 有 Tushare 2000+积分 → 优先 Tushare
- 无积分或积分不足 → 使用 AkShare
- Tushare 调用失败 → 降级到 AkShare

---

#### 7.5.3 公司基本信息

**选择策略**：两者字段互补，可同时使用

| 数据源 | 提供信息 |
|--------|---------|
| **Tushare** | 上市日期、交易所、市场类型 |
| **AkShare** | 公司描述、主营业务、联系方式 |

---

#### 7.5.4 交易日历

**选择策略**：只使用 Tushare（官方数据，权威），AkShare 无专门接口

---

#### 7.5.5 实时行情

**选择策略**：

| 场景 | 推荐数据源 | 原因 |
|------|-----------|------|
| **盘中实时** | AkShare | 数据更新快（5分钟级别） |
| **收盘后** | Tushare 或 AkShare 均可 | 数据一致 |

---

### 7.6 关键字段单位转换表

| 数据项 | Tushare 单位 | AkShare 单位 | 标准单位 | 转换规则 |
|--------|-------------|-------------|---------|---------|
| **成交量** | 手（1手=100股） | 股或手 | 股 | Tushare: vol × 100 |
| **成交额** | 千元 | 元 | 元 | Tushare: amount × 1000 |
| **涨跌幅** | % | % | % | 直接使用（如 5.23 表示 5.23%） |
| **市值** | 万元 | 元 | 元 | Tushare: × 10000 |
| **净利润** | 元 | 元 | 元 | 直接使用 |
| **总资产** | 元 | 元 | 元 | 直接使用 |

**重点注意**：
- ⚠️ Tushare 成交量单位是**手**，需要乘以 100 转换为股
- ⚠️ Tushare 成交额单位是**千元**，需要乘以 1000 转换为元
- ⚠️ AkShare 不同接口单位可能不一致，需要逐个接口确认

---

### 7.7 实际应用场景

#### 7.7.1 场景1：首次全量同步A股数据

**需求**：系统初始化，需要获取所有A股的基本信息和当日行情

**数据源选择**：
- 使用 Tushare 获取所有股票列表（结构化数据，字段完整）
- 使用 AkShare 获取当日实时行情（一次性返回所有股票）

**原因**：
- Tushare 股票列表数据完整稳定
- AkShare 实时行情一次性返回全部，无需循环调用
- 避免触发 Tushare 限流

---

#### 7.7.2 场景2：用户添加自选股，同步1年历史K线

**需求**：用户添加股票到自选，需要同步近1年K线（约250条数据）

**数据源选择**：
- 优先使用 Tushare 获取前复权日K线

**原因**：
- Tushare 历史数据稳定，支持复权
- 单次可获取全部数据，无需分页
- 数据质量高，减少后续问题

---

#### 7.7.3 场景3：用户添加美股自选，Tushare积分不足

**需求**：用户添加美股，但 Tushare 积分不足或超限流

**数据源选择**：
- Tushare 调用失败，自动降级到 AkShare

**原因**：
- AkShare 完全免费，无积分限制
- 美股数据质量与 Yahoo Finance 一致
- 多数据源降级保证可用性

---

#### 7.7.4 场景4：定时任务同步当日行情

**需求**：每天定时同步所有A股当日行情

**数据源选择**：
- 使用 AkShare 获取全部股票行情（一次性返回）

**原因**：
- AkShare 一次性返回全部股票，效率最高
- 避免循环调用 Tushare（5000次调用会触发限流）
- 免费且数据及时

---

### 7.8 数据源配置最佳实践

#### 7.8.1 A股市场配置

**标准配置**（推荐）：
```yaml
A股数据源配置：
├─ 数据源1：Tushare（优先级1）
│  ├─ 适用：历史K线、财务报表、交易日历
│  ├─ 积分要求：2000+
│  └─ 限流：200次/分钟
│
├─ 数据源2：AkShare（优先级2）
│  ├─ 适用：实时行情、板块数据、分钟线
│  ├─ 免费无积分
│  └─ 限流：60次/分钟（自行控制）
│
└─ 数据源3：东方财富（优先级3，降级）
   ├─ 适用：紧急降级
   └─ 限流：30次/分钟
```

**低成本配置**（无积分）：
```yaml
A股数据源配置：
├─ 数据源1：AkShare（优先级1，主力）
│  ├─ 适用：所有数据类型
│  └─ 限流：60次/分钟
│
└─ 数据源2：东方财富（优先级2，备用）
   └─ 限流：30次/分钟
```

#### 7.8.2 港股/美股市场配置

**港股配置**：
```yaml
港股数据源配置：
├─ 数据源1：AkShare（优先级1）
│  ├─ 数据全面、免费
│  └─ 限流：60次/分钟
│
└─ 数据源2：Tushare（优先级2，如有积分）
   ├─ 需要2000+积分
   └─ 限流：200次/分钟
```

**美股配置**：
```yaml
美股数据源配置：
├─ 数据源1：AkShare（优先级1）
│  ├─ 内部调用 Yahoo Finance
│  └─ 限流：60次/分钟
│
└─ 数据源2：Tushare（优先级2，如有积分）
   ├─ 需要2000+积分
   └─ 限流：200次/分钟
```

---

## 八、核心业务逻辑设计

### 8.1 数据源配置模型

#### 8.1.1 配置结构

```yaml
数据源配置：
  source_id: "tushare"
  market: "A_STOCK"
  enabled: true
  priority: 1  # 优先级（数字越小优先级越高）

  # 数据源配置
  config:
    api_key: "encrypted_api_key"  # 加密存储
    base_url: "https://api.tushare.pro"
    timeout: 30  # 超时时间（秒）

  # 限流配置
  rate_limit:
    max_requests_per_minute: 1000
    max_requests_per_day: 10000

  # 数据类型支持
  supported_data_types:
    - "quote"      # 行情
    - "history"     # 历史K线
    - "financials"  # 财务报表
    - "company"     # 公司信息

  # 元数据
  metadata:
    created_at: datetime
    updated_at: datetime
    last_test_status: "success"  # success | failed
    last_tested_at: datetime
```

#### 8.1.2 多数据源优先级

```
每个市场可配置多个数据源，按优先级排序：

A股数据源示例：
├─ 1. Tushare（优先级1，主数据源）
├─ 2. 东方财富（优先级2，备用数据源）
└─ 3. 新浪财经（优先级3，降级数据源）

数据获取流程：
├─ 尝试优先级1：Tushare
│   ├─ 成功 → 返回数据
│   └─ 失败 → 降级到优先级2
├─ 尝试优先级2：东方财富
│   ├─ 成功 → 返回数据
│   └─ 失败 → 降级到优先级3
└─ 尝试优先级3：新浪财经
    ├─ 成功 → 返回数据
    └─ 失败 → 返回错误
```

---

### 8.2 数据获取流程

#### 8.2.1 按需更新流程

```
用户请求股票数据
    ↓
步骤1：检查数据库
├─ 查询条件：股票代码、市场、日期
├─ 判断数据是否存在
└─ 判断数据是否过期
    ↓
步骤2：数据存在且未过期
└─ 直接返回（最快）
    ↓
步骤3：数据不存在或已过期
├─ 尝试获取分布式锁（防止并发重复获取）
├─ 获取到锁 → 调用数据源接口
│   ├─ 按优先级尝试数据源
│   ├─ 数据验证
│   ├─ 标准化字段
│   ├─ 存入数据库
│   └─ 释放锁
└─ 未获取到锁 → 等待2秒，再次检查数据库
    └─ 其他用户已更新，直接返回
```

#### 8.2.2 分布式锁工作原理

**防止并发重复获取**：

```
场景：100个用户同时请求 AAPL 数据（缓存已过期）

没有分布式锁：
└─ 100个用户都调用API获取数据
    └─ 浪费99次API调用

有分布式锁：
├─ 用户1获取到锁 → 调用API获取数据
├─ 用户2-100未获取到锁 → 等待2秒
├─ 用户1获取完数据，释放锁
└─ 用户2-100等待结束，从数据库获取数据
    └─ 只调用1次API，节省99次
```

**锁的配置**：
| 参数 | 值 | 说明 |
|------|-----|------|
| 键格式 | `lock:data_fetch:{market}:{symbol}` | 例如：lock:data_fetch:A_STOCK:AAPL |
| 超时时间 | 30秒 | 防止死锁 |
| 等待时间 | 2秒 | 未获取锁的用户等待时间 |

---

### 8.3 数据验证机制

#### 8.3.1 验证流程

```
从API获取的原始数据
    ↓
步骤1：字段标准化
├─ 不同数据源字段名不同
├─ 统一为标准字段名
└─ 示例：Open/OPEN → open
    ↓
步骤2：完整性检查
├─ 检查必填字段
├─ 必填字段：open, high, low, close, volume
└─ 缺少 → 拒绝数据，尝试备用数据源
    ↓
步骤3：合理性检查
├─ 价格必须 > 0
├─ 成交量必须 >= 0
└─ 不合理 → 拒绝数据
    ↓
步骤4：一致性检查
├─ high >= low
├─ high >= open, high >= close
├─ low <= open, low <= close
└─ 不一致 → 拒绝数据
    ↓
步骤5：数据质量评分
├─ 完整性：40分
├─ 合理性：30分
└─ 一致性：30分
    ↓
验证通过的数据
├─ 质量评分 > 70分 → 存入数据库
└─ 质量评分 <= 70分 → 记录警告，但仍然存入
```

#### 8.3.2 验证规则详解

**完整性检查**：
```
必填字段：
├─ open（开盘价）
├─ high（最高价）
├─ low（最低价）
├─ close（收盘价）
└─ volume（成交量）

可选字段：
├─ amount（成交额）
├─ pe_ratio（市盈率）
└─ turnover_rate（换手率）
```

**一致性检查**：
```
规则1：high >= low
└─ 最高价必须 >= 最低价

规则2：high >= open 且 high >= close
└─ 最高价必须 >= 开盘价和收盘价

规则3：low <= open 且 low <= close
└─ 最低价必须 <= 开盘价和收盘价
```

**数据质量评分**：
```
总分：100分

维度1：完整性（40分）
└─ 必填字段完整率 × 40

维度2：合理性（30分）
└─ 价格、成交量合理性

维度3：一致性（30分）
└─ 价格关系一致性

评分等级：
├─ 90-100分：高质量
├─ 70-89分：中等质量
└─ <70分：低质量（记录警告）
```

---

### 8.4 限流策略

#### 8.4.1 为什么需要限流？

**场景**：数据源有调用频率限制

```
Yahoo Finance 免费API：
└─ 限制：2000次/小时
    └─ 超过 → IP被封锁，1小时无法使用

Tushare 免费API：
└─ 限制：1000次/分钟，10000次/天
    └─ 超过 → 返回错误，无法获取数据
```

#### 8.4.2 限流算法：令牌桶

**原理**：
```
令牌桶配置：
├─ 桶容量：100个令牌
├─ 补充速率：1个/秒
└─ 消耗规则：1次请求消耗1个令牌

工作流程：
├─ 正常情况：每秒1个请求（消耗1个，补充1个，保持平衡）
├─ 突发情况：可以瞬间发送100个请求（桶满了）
└─ 突发后：要等100秒才能恢复到100个令牌
```

**优点**：
- ✅ 允许突发流量（批量任务）
- ✅ 平滑限流（不会在时间边界突然重置）
- ✅ 适合定时任务场景

#### 8.4.3 限流配置

```
A股数据源限流：
├─ Tushare：1000次/分钟，10000次/天
├─ 东方财富：500次/分钟（保守估计）
└─ 新浪财经：500次/分钟

美股数据源限流：
├─ Yahoo Finance：1800次/小时（留200次余量）
└─ Alpha Vantage：5次/分钟（免费版限制）

港股数据源限流：
├─ Yahoo Finance：1800次/小时
└─ 东方财富：500次/分钟
```

---

## 十一、数据库设计

> **详细数据库设计文档**：请参阅 [《数据库设计.md》](./数据库设计.md)

### 11.1 设计概述

数据库设计已独立成文档，包含以下内容：

**核心表结构**：
- `system_data_sources` - 系统公共数据源配置表
- `user_data_sources` - 用户数据源配置表
- `stock_quotes` - 股票当日行情表
- `stock_klines` - 历史日K线表
- `stock_info` - 股票基本信息表
- `stock_financials` - 财务报表表
- `stock_companies` - 公司信息表
- `stock_news` - 历史新闻表
- `user_watchlists` - 用户自选股表
- `data_sync_tasks` - 数据同步任务表
- `write_limit_records` - 写入限流记录表

**关键设计要点**：
1. **数据保留策略**：所有数据只保留1年，定时任务自动清理过期数据
2. **盘中数据标识**：`is_complete` 字段区分盘中数据（false）和收盘数据（true）
3. **增量更新逻辑**：第二次及以后获取数据时对比差异，增量更新
4. **写入限流控制**：按股票代码+数据类型+1分钟时间窗口限流
5. **唯一索引设计**：防止重复数据写入
6. **TTL索引**：限流记录1分钟后自动过期

**数据库命名规范**：
- 数据库名：`stock_analysis`
- 集合命名：小写字母+下划线（如 `stock_quotes`）
- 字段命名：小写下划线命名法（如 `trade_date`）

### 11.2 数据库设计文档引用

**完整数据库设计请参阅**：[《数据库设计.md》](./数据库设计.md)

该文档包含：
- 完整的表结构定义
- 索引设计（唯一索引、查询索引、TTL索引）
- 数据保留与清理策略
- 数据完整性保障
- 数据库分片策略
- 数据库备份策略

---

## 十二、技术指标计算

### 12.1 计算策略

**决策**：技术指标在**后端计算**并存储到数据库。

**原因**：
1. **一致性**：所有用户看到相同的指标值
2. **性能**：避免前端重复计算
3. **复用性**：AI 工具可以直接使用计算好的指标
4. **精确性**：后端可以使用高精度计算库

**计算时机**：
- 日K线数据入库时自动计算
- 历史数据补全后批量计算

---

### 10.2 支持的技术指标

#### 10.2.1 移动平均线（MA）

| 指标 | 计算周期 | 说明 |
|------|---------|------|
| MA5 | 5日 | 短期均线 |
| MA10 | 10日 | 短期均线 |
| MA20 | 20日 | 中期均线 |
| MA60 | 60日 | 长期均线 |
| MA120 | 120日 | 半年线 |
| MA250 | 250日 | 年线 |

**计算公式**：
```
MA(N) = (C1 + C2 + ... + CN) / N

其中：
- C1, C2, ..., CN 为最近 N 日收盘价
- N 为计算周期
```

---

#### 10.2.2 MACD 指标

| 指标 | 参数 | 说明 |
|------|------|------|
| MACD | (12, 26, 9) | 默认参数 |
| DIF | 12日EMA - 26日EMA | 快线 |
| DEA | DIF的9日EMA | 慢线 |
| MACD柱 | (DIF - DEA) × 2 | 柱状图 |

**计算公式**：
```
EMA(N) = 前一日EMA × (N-1)/(N+1) + 今日收盘价 × 2/(N+1)

DIF = EMA(12) - EMA(26)
DEA = DIF 的 9日EMA
MACD柱 = (DIF - DEA) × 2
```

---

#### 10.2.3 RSI 指标

| 指标 | 参数 | 说明 |
|------|------|------|
| RSI6 | 6日 | 短期超买超卖 |
| RSI12 | 12日 | 中期超买超卖 |
| RSI24 | 24日 | 长期超买超卖 |

**计算公式**：
```
RS = N日内收盘涨幅平均值 / N日内收盘跌幅平均值
RSI = 100 - 100 / (1 + RS)

取值范围：0 ~ 100
- RSI > 70：超买区域
- RSI < 30：超卖区域
```

---

#### 10.2.4 KDJ 指标

| 指标 | 参数 | 说明 |
|------|------|------|
| K值 | 9日RSV的3日平滑 | 快速指标 |
| D值 | K值的3日平滑 | 慢速指标 |
| J值 | 3K - 2D | 超买超卖 |

**计算公式**：
```
RSV = (今日收盘价 - 9日最低价) / (9日最高价 - 9日最低价) × 100

K = 2/3 × 前一日K + 1/3 × 今日RSV
D = 2/3 × 前一日D + 1/3 × 今日K
J = 3K - 2D

取值范围：
- K, D：0 ~ 100
- J：可能超出 0 ~ 100
```

---

#### 10.2.5 布林带（BOLL）

| 指标 | 参数 | 说明 |
|------|------|------|
| 中轨 | 20日MA | 移动平均线 |
| 上轨 | 中轨 + 2倍标准差 | 压力线 |
| 下轨 | 中轨 - 2倍标准差 | 支撑线 |

**计算公式**：
```
中轨 = MA(20)
标准差 = SQRT(Σ(Ci - MA)² / 20)
上轨 = 中轨 + 2 × 标准差
下轨 = 中轨 - 2 × 标准差
```

---

### 10.3 计算流程

```
日K线数据入库
    ↓
触发技术指标计算
    ↓
检查历史数据是否充足
├─ MA250 需要至少 250 条历史数据
├─ 数据不足 → 计算可用的指标，其他标记为 null
└─ 数据充足 → 计算所有指标
    ↓
计算各项指标
├─ MA系列（需要的历史数据最少）
├─ MACD
├─ RSI
├─ KDJ
└─ BOLL
    ↓
更新数据库中的 indicators 字段
    ↓
计算完成
```

---

## 十三、API 端点设计

### 13.1 管理员专用 API

**权限**：仅管理员（ADMIN / SUPER_ADMIN）

#### 11.1.1 数据源配置管理

**1. 列出所有数据源配置**

```
GET /api/admin/market-data/sources

查询参数：
├─ market: A_STOCK | US_STOCK | HK_STOCK（可选）
└─ enabled: true | false（可选）

响应：
{
  "sources": [
    {
      "_id": "config_id_1",
      "source_id": "tushare",
      "market": "A_STOCK",
      "enabled": true,
      "priority": 1,
      "rate_limit": {...},
      "supported_data_types": ["quote", "history"],
      "metadata": {...}
    }
  ]
}
```

**2. 创建数据源配置**

```
POST /api/admin/market-data/sources

请求体：
{
  "source_id": "tushare",
  "market": "A_STOCK",
  "enabled": true,
  "priority": 1,
  "config": {
    "api_key": "user_token_here"
  },
  "rate_limit": {
    "max_requests_per_minute": 1000
  }
}

响应：创建的配置详情
```

**3. 更新数据源配置**

```
PUT /api/admin/market-data/sources/{config_id}

请求体：
{
  "enabled": true,
  "priority": 1,
  "config": {
    "api_key": "new_token_here"
  }
}
```

**4. 删除数据源配置**

```
DELETE /api/admin/market-data/sources/{config_id}
```

**5. 测试数据源连接**

```
POST /api/admin/market-data/sources/{config_id}/test

响应：
{
  "success": true,
  "message": "连接成功",
  "test_time": "2025-01-01T10:00:00Z",
  "response_time_ms": 150
}
```

---

#### 11.1.2 数据管理

**1. 手动触发数据更新**

```
POST /api/admin/market-data/data/refresh

请求体：
{
  "symbol": "600000.SH",
  "market": "A_STOCK",
  "data_types": ["quote", "history"]
}

响应：
{
  "success": true,
  "message": "数据更新任务已提交"
}
```

**2. 查看数据统计**

```
GET /api/admin/market-data/data/stats

响应：
{
  "total_symbols": 5000,
  "total_records": 1500000,
  "data_sources": {
    "tushare": 800000,
    "yahoo": 700000
  },
  "markets": {
    "A_STOCK": 700000,
    "US_STOCK": 600000,
    "HK_STOCK": 200000
  }
}
```

---

### 13.2 用户数据查询 API

**权限**：所有认证用户

#### 11.2.1 获取当日行情

```
GET /api/market-data/quote/{symbol}

查询参数：
├─ market: A_STOCK | US_STOCK | HK_STOCK
└─ date: YYYY-MM-DD（可选，默认最新）

响应：
{
  "symbol": "600000.SH",
  "market": "A_STOCK",
  "trade_date": "2025-01-01",
  "open": 10.5,
  "high": 10.8,
  "low": 10.3,
  "close": 10.7,
  "volume": 1000000,
  "data_source": "tushare",
  "fetched_at": "2025-01-01T10:00:00Z",
  "data_quality": 85
}
```

#### 11.2.2 获取历史K线

```
GET /api/market-data/klines/{symbol}

查询参数：
├─ market: A_STOCK | US_STOCK | HK_STOCK
├─ start_date: YYYY-MM-DD（必填）
├─ end_date: YYYY-MM-DD（可选，默认今天）
└─ include_indicators: true | false（是否包含技术指标，默认true）

响应：
{
  "symbol": "600000.SH",
  "market": "A_STOCK",
  "klines": [
    {
      "trade_date": "2024-12-01",
      "open": 10.5,
      "high": 10.8,
      "low": 10.3,
      "close": 10.7,
      "volume": 1000000,
      "amount": 10700000,
      "change_pct": 2.88,
      "indicators": {
        "ma5": 10.6,
        "ma10": 10.4,
        "macd_dif": 0.05,
        "rsi6": 55.5,
        "kdj_k": 65.2
        // ...
      }
    }
    // ...
  ],
  "total": 250,
  "data_source": "tushare"
}
```

#### 11.2.3 获取财务报表

```
GET /api/market-data/financials/{symbol}

查询参数：
├─ market: A_STOCK | US_STOCK | HK_STOCK
└─ report_type: Q1 | Q2 | Q3 | Q4 | annual（可选）

响应：
{
  "symbol": "600000.SH",
  "market": "A_STOCK",
  "financials": [
    {
      "report_type": "Q3",
      "report_date": "2024-09-30",
      "publish_date": "2024-10-30",
      "income_statement": {...},
      "balance_sheet": {...},
      "cash_flow": {...},
      "financial_ratios": {...}
    }
  ]
}
```

#### 11.2.4 获取公司信息

```
GET /api/market-data/company/{symbol}

查询参数：
├─ market: A_STOCK | US_STOCK | HK_STOCK

响应：
{
  "symbol": "600000.SH",
  "market": "A_STOCK",
  "company_name": "浦发银行",
  "industry": "银行",
  "sector": "金融",
  "list_date": "1999-11-10",
  "exchange": "SSE",
  "contact": {...},
  "business": {...},
  "capital_structure": {...}
}
```

#### 11.2.5 获取股票新闻

```
GET /api/market-data/news/{symbol}

查询参数：
├─ market: A_STOCK | US_STOCK | HK_STOCK
├─ start_date: YYYY-MM-DD（可选）
├─ end_date: YYYY-MM-DD（可选）
├─ category: company | industry | market | macro（可选）
├─ page: 页码，默认1
└─ page_size: 每页条数，默认20，最大100

响应：
{
  "symbol": "600000.SH",
  "news": [
    {
      "news_id": "abc123",
      "title": "浦发银行发布三季报",
      "summary": "净利润同比增长10%",
      "source": "上海证券报",
      "publish_time": "2024-10-30T10:00:00Z",
      "category": "company",
      "sentiment": {
        "score": 0.75,
        "label": "positive"
      }
    }
    // ...
  ],
  "total": 120,
  "page": 1,
  "page_size": 20
}
```

---

### 13.3 用户数据源配置 API

**权限**：所有认证用户（操作自己的自选股）

#### 11.3.1 获取自选股列表

```
GET /api/market-data/watchlist

响应：
{
  "stocks": [
    {
      "symbol": "600000.SH",
      "market": "A_STOCK",
      "name": "浦发银行",
      "added_at": "2025-01-01T10:00:00Z",
      "sync_status": "completed",
      "last_sync_at": "2025-01-02T08:00:00Z",
      "sync_progress": 100,
      "config": {
        "history_range": "1y",
        "news_range": "1y",
        "financial_periods": 4
      },
      // 最新行情快照
      "latest_quote": {
        "trade_date": "2025-01-02",
        "close": 10.7,
        "change_pct": 2.88
      }
    }
    // ...
  ],
  "total": 15
}
```

#### 11.3.2 添加自选股

```
POST /api/market-data/watchlist

请求体：
{
  "symbol": "600000.SH",
  "market": "A_STOCK",
  "notes": "银行龙头",  // 可选
  "config": {
    "history_range": "1y",  // 可选，默认1y
    "news_range": "1y",      // 可选，默认1y
    "financial_periods": 4   // 可选，默认4
  }
}

响应：
{
  "success": true,
  "message": "添加成功，数据同步已开始",
  "sync_task_id": "task_123"
}

说明：
- 添加后立即触发完整数据同步（历史K线、新闻、财报）
- 同步为异步任务，不阻塞响应
```

#### 11.3.3 移除自选股

```
DELETE /api/market-data/watchlist/{symbol}

查询参数：
├─ market: A_STOCK | US_STOCK | HK_STOCK

响应：
{
  "success": true,
  "message": "已移除"
}

说明：
- 只移除自选关系，不删除数据库中的数据
```

#### 11.3.4 更新自选股配置

```
PUT /api/market-data/watchlist/{symbol}/config

查询参数：
├─ market: A_STOCK | US_STOCK | HK_STOCK

请求体：
{
  "history_range": "2y",
  "news_range": "1y",
  "financial_periods": 8
}

响应：
{
  "success": true,
  "message": "配置已更新，将补充同步历史数据"
}

说明：
- 如果扩大了时间范围，会触发增量同步
- 如果缩小了时间范围，不会删除已有数据
```

#### 11.3.5 查询同步任务状态

```
GET /api/market-data/watchlist/{symbol}/sync-status

查询参数：
├─ market: A_STOCK | US_STOCK | HK_STOCK

响应：
{
  "symbol": "600000.SH",
  "sync_status": "running",  // pending | syncing | completed | failed
  "progress": 65,
  "current_task": "syncing_klines",  // syncing_klines | syncing_news | syncing_financials
  "result": {
    "kline_count": 180,
    "news_count": 0,
    "financials_count": 4
  },
  "started_at": "2025-01-02T10:00:00Z",
  "estimated_completion": "2025-01-02T10:05:00Z"
}
```

---

## 十四、与 TradingAgents 集成方案

### 14.1 工具注册方式

**位置**：`backend/modules/market_data/tools/`

**集成方式**：注册到 `trading_agents` 的本地工具

### 14.2 工具列表

#### 12.2.1 A股工具

```
工具名：get_a_stock_quote

功能：获取A股行情数据

参数：
├─ symbol: 股票代码（如 600000.SH）
└─ date: 日期（可选，默认最新）

返回：行情数据字典

数据获取流程：
├─ 1. 检查数据库
│   ├─ 存在且未过期 → 直接返回
│   └─ 不存在或已过期 → 进入步骤2
├─ 2. 获取分布式锁
│   ├─ 成功 → 调用API获取数据
│   └─ 失败 → 等待2秒，再次查数据库
└─ 3. 返回数据
```

#### 12.2.2 美股工具

```
工具名：get_us_stock_quote

功能：获取美股行情数据

参数：
├─ symbol: 股票代码（如 AAPL）
└─ date: 日期（可选，默认最新）

返回：行情数据字典
```

#### 12.2.3 港股工具

```
工具名：get_hk_stock_quote

功能：获取港股行情数据

参数：
├─ symbol: 股票代码（如 0700.HK）
└─ date: 日期（可选，默认最新）

返回：行情数据字典
```

---

## 十五、并发与限流策略

### 15.1 分布式锁

**目的**：防止并发请求重复获取数据

**工作流程**：
```
并发场景：100个用户同时请求 AAPL 数据

时间线：
├─ T+0秒：用户1-100同时请求数据
├─ T+0.001秒：用户1获取到分布式锁
├─ T+0.002秒：用户2-100未获取到锁，进入等待
├─ T+0.100秒：用户1调用API获取数据
├─ T+0.500秒：用户1存入数据库，释放锁
├─ T+2.002秒：用户2-100等待结束，从数据库获取数据
└─ 结果：只调用1次API，节省99次
```

**锁配置**：
| 参数 | 值 |
|------|-----|
| 键格式 | `lock:data_fetch:{market}:{symbol}` |
| 超时时间 | 30秒 |
| 等待时间 | 2秒 |

---

### 15.2 限流算法

**算法**：令牌桶（Token Bucket）

**配置**：
```
A股数据源：
├─ Tushare：1000次/分钟
└─ 东方财富：500次/分钟

美股数据源：
└─ Yahoo Finance：1800次/小时

港股数据源：
└─ Yahoo Finance：1800次/小时
```

**工作流程**：
```
请求API前：
├─ 1. 检查令牌桶是否有令牌
│   ├─ 有令牌 → 消耗1个，允许请求
│   └─ 无令牌 → 拒绝请求，等待降级到备用数据源
└─ 2. 定期补充令牌（每秒1个）
```

---

## 十六、错误处理与降级

### 16.1 多数据源降级策略

**降级流程**：
```
主数据源失败
    ↓
尝试备用数据源1
    ├─ 成功 → 返回数据
    └─ 失败 → 进入下一步
        ↓
尝试备用数据源2
    ├─ 成功 → 返回数据
    └─ 失败 → 返回错误
```

**降级触发条件**：
- ✅ 网络超时（>30秒）
- ✅ 接口返回错误（HTTP 5xx）
- ✅ 数据解析失败
- ✅ 数据验证失败

**降级日志记录**：
```
日志内容：
├─ 尝试的数据源
├─ 失败原因
├─ 降级到哪个数据源
└─ 最终结果
```

---

### 16.2 错误分类与处理

| 错误类型 | 处理方式 | 是否重试 |
|---------|---------|---------|
| 网络超时 | 降级到备用数据源 | ✅ 是 |
| API限流（429） | 等待后重试 | ✅ 是 |
| 数据验证失败 | 降级到备用数据源 | ❌ 否 |
| 所有数据源失败 | 返回明确错误 | ❌ 否 |

---

## 十七、定时任务设计

### 17.1 任务调度

**调度器**：APScheduler（已集成到项目）

### 17.2 定时任务列表

#### 15.2.1 A股数据更新

**说明**：只同步轻量数据，不同步历史K线、历史新闻等重量数据

```
任务1：当日行情更新
├─ 时间：每天 15:30（收盘后）
├─ 任务：拉取所有A股的当日行情
├─ 数据类型：quote（当日）
└─ 存储表：stock_quotes

任务2：公司信息检查
├─ 时间：每周一 16:00
├─ 任务：检查公司基本信息是否有变更
├─ 数据类型：company
└─ 存储表：stock_companies

任务3：财务指标更新
├─ 时间：每天 16:30
├─ 任务：检查是否有新的财务指标发布
├─ 数据类型：financials（仅最新一期）
└─ 存储表：stock_financials

注意：
- 不同步历史K线（由自选股触发）
- 不同步历史新闻（由自选股触发）
```

#### 15.2.2 美股数据更新

**说明**：只同步轻量数据，不同步历史K线、历史新闻等重量数据

```
任务1：当日行情更新
├─ 时间：每天 05:00（收盘后，北京时间）
├─ 任务：拉取所有美股的当日行情
├─ 数据类型：quote（当日）
└─ 存储表：stock_quotes

任务2：公司信息检查
├─ 时间：每周一 05:30
├─ 任务：检查公司基本信息是否有变更
├─ 数据类型：company
└─ 存储表：stock_companies

任务3：财务指标更新
├─ 时间：每天 06:00
├─ 任务：检查是否有新的财务指标发布
├─ 数据类型：financials（仅最新一期）
└─ 存储表：stock_financials

注意：
- 不同步历史K线（由自选股触发）
- 不同步历史新闻（由自选股触发）
```

#### 15.2.3 港股数据更新

**说明**：只同步轻量数据，不同步历史K线、历史新闻等重量数据

```
任务1：当日行情更新
├─ 时间：每天 16:30（收盘后）
├─ 任务：拉取所有港股的当日行情
├─ 数据类型：quote（当日）
└─ 存储表：stock_quotes

任务2：公司信息检查
├─ 时间：每周一 17:00
├─ 任务：检查公司基本信息是否有变更
├─ 数据类型：company
└─ 存储表：stock_companies

任务3：财务指标更新
├─ 时间：每天 17:30
├─ 任务：检查是否有新的财务指标发布
├─ 数据类型：financials（仅最新一期）
└─ 存储表：stock_financials

注意：
- 不同步历史K线（由自选股触发）
- 不同步历史新闻（由自选股触发）
```

### 17.3 任务失败处理

```
任务失败处理策略：
├─ 记录失败日志
├─ 发送告警通知（邮件）
├─ 下次定时任务继续尝试
└─ 手动触发更新（管理员）
```

---

## 十八、总结

### 18.1 核心设计原则

1. **管理员统一配置**：所有数据源由管理员配置，用户无需关心
2. **本地化存储**：数据存储在本地数据库，用户请求不直接调用外部API
3. **分层同步策略**：
   - 定时同步：轻量数据（当日行情、公司信息）
   - 自选股同步：完整数据（历史K线、新闻、财报）
   - 按需同步：数据缺失或过期时触发
4. **缓存命中机制**：数据库作为数据中转站，首次请求触发同步，后续复用
5. **多数据源降级**：主数据源失败自动切换备用数据源
6. **并发控制**：分布式锁防止重复获取
7. **数据标准化**：统一数据模型，ETL 流程处理
8. **技术指标后端计算**：保证一致性和复用性

---

### 16.2 关键技术点

| 技术点 | 说明 |
|--------|------|
| **统一数据模型** | 以 Tushare 为参考标准，定义标准字段 |
| **ETL 流程** | Extract-Transform-Load 三阶段数据处理 |
| **字段映射** | Tushare/AkShare → 标准字段的转换规则 |
| **股票代码标准化** | 统一为 {代码}.{后缀} 格式 |
| **分布式锁** | 防止并发重复获取数据 |
| **令牌桶限流** | 保护API配额 |
| **数据验证** | 完整性、合理性、一致性三层验证 |
| **技术指标计算** | MA/MACD/RSI/KDJ/BOLL 后端计算并存储 |
| **多数据源降级** | 按优先级自动切换数据源 |

---

### 16.3 数据同步策略总结

| 同步类型 | 触发方式 | 同步内容 | 数据范围 | 时间范围 |
|---------|---------|---------|---------|---------|
| 定时同步 | 定时任务 | 轻量数据 | 所有股票 | 当日 |
| 自选股同步 | 用户添加 | 完整数据 | 指定股票 | 用户配置（默认1年） |
| 按需同步 | 数据缺失/过期 | 指定数据 | 指定股票 | 按需 |

---

### 16.4 数据源推荐配置

**A股**：
- 优先级1：Tushare（数据最全、最稳定）
- 优先级2：AkShare（免费备用）
- 优先级3：东方财富（降级）

**美股**：
- 优先级1：Yahoo Finance
- 优先级2：AkShare（内部调用 Yahoo Finance）

**港股**：
- 优先级1：AkShare
- 优先级2：Yahoo Finance

---

### 16.5 预期收益

| 收益项 | 描述 |
|--------|------|
| **用户体验** | 数据从本地数据库读取，响应快（缓存命中） |
| **系统稳定性** | 多数据源降级，单点故障不影响使用 |
| **成本可控** | 定时同步+按需同步控制API调用次数 |
| **数据质量** | ETL流程+数据验证确保准确性 |
| **数据一致性** | 统一数据模型，技术指标后端计算 |
| **扩展性** | 新增数据源只需实现适配器和字段映射 |

---

## 附录

### A. 数据源对比

| 数据源 | 市场 | 免费额度 | 数据质量 | 稳定性 | 推荐优先级 |
|--------|------|---------|---------|--------|-----------|
| Tushare | A股/港股/美股 | 需积分（基础免费） | ⭐⭐⭐⭐⭐ | 高 | A股首选 |
| AkShare | 全市场 | 完全免费 | ⭐⭐⭐⭐ | 中 | 备用/港股首选 |
| Yahoo Finance | 美股/港股 | 完全免费 | ⭐⭐⭐⭐ | 高 | 美股首选 |
| 东方财富 | A股/港股 | 完全免费 | ⭐⭐⭐ | 中 | 降级方案 |

### B. 参考资料

- [Tushare 官方文档](https://tushare.pro)
- [AkShare 官方文档](https://akshare.akfamily.xyz)
- [Yahoo Finance API](https://finance.yahoo.com)
- [Alpha Vantage 文档](https://www.alphavantage.co)

### C. 本项目接口文档

**详细接口说明请参考**：
- [Tushare 接口文档](./tushare接口文档.md) - 包含34个核心接口的详细说明、参数、返回字段、示例
- [AkShare 接口文档](./akshare接口文档.md) - 包含80+接口的完整说明、最佳实践、错误处理

### D. 相关文档

- [项目 README.md](../../README.md)
- [CLAUDE.md 项目记忆文档](../../CLAUDE.md)

---

### E. 快速参考

#### E.1 关键配置参数

| 参数 | Tushare | AkShare |
|------|---------|---------|
| API地址 | https://api.tushare.pro | 无需配置 |
| 认证方式 | Token | 无需认证 |
| 推荐频率 | 200次/分钟（2000积分） | 60次/分钟（自控） |
| 超时配置 | 30秒 | 30秒 |
| 重试次数 | 3次 | 3次 |

#### E.2 核心API速查

**最常用接口（必须实现）**：
- ✅ 日K线：Tushare `pro_bar` / AkShare `stock_zh_a_hist`
- ✅ 公司信息：Tushare `stock_basic` / AkShare `stock_individual_info_em`
- ✅ 财务报表：Tushare `income` / AkShare `stock_profit_sheet_by_report_em`
- ✅ 实时行情：AkShare `stock_zh_a_spot_em` （推荐）
- ✅ 交易日历：Tushare `trade_cal` （Tushare独占）

#### E.3 数据量估算

| 数据类型 | 单只股票（1年） | 全市场（5000只） |
|---------|----------------|-----------------|
| 日K线 | 250条 ≈ 50KB | 1.25GB |
| 历史新闻 | 500条 ≈ 2.5MB | 12.5TB（按需同步） |
| 财务报表 | 4期 ≈ 40KB | 200MB |
| 公司信息 | 1条 ≈ 1KB | 5MB |

**存储策略**：
- 定时同步：只同步轻量数据（~1.5GB）
- 自选股同步：按用户需求同步重量数据
- 总存储需求：初期约2-5GB，长期增长受自选股数量影响

---

**文档结束**
